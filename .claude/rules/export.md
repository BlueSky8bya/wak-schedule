# 포스터 표면 규칙

- 포스터의 캡처 대상 표면은 `[data-export-surface]`로 표시한다.
- 시청자 화면(포스터)에는 편집 컨트롤·관리자 UI가 렌더되지 않는다.
- 표면 기하(고정 폭 1840, 내용에 따라 세로로 자람)는 모드가 달라도 동일하다 —
  화면 폭에는 표면을 통째로 축소(transform: scale)해 맞춘다. 표면 내부를 재배치하지 않는다.
