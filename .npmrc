# @vercel/analytics가 선언한 '선택적' @sveltejs/kit peer 때문에 npm이 svelte/vite@8
# 체인을 끌어와 이 프로젝트의 vite@5(vitest용)와 충돌한다(ERESOLVE). Next.js 앱엔
# 무관한 충돌이라 peer 해석을 완화한다. Vercel의 clean install에서도 빌드가 되게 커밋.
legacy-peer-deps=true
